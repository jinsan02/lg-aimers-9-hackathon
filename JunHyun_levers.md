# CLAUDE 제안 LB 실측 실험 사전등록 — 공개 리더보드 포물선 적합

작성 2026-08-16. 결과를 읽기 전에 실험 방향·arm·채택 기준을 여기 고정한다.
결과가 나온 뒤에는 §7 결과표에 append만 하고 본문은 수정하지 않는다.

## 1. 맥락 — 왜 지금 이것인가

- 챔피언 **B1S8, LB 1108.433 (#34)**, 마감 2026-09-01. 오프라인 실험 큐는
  2026-08-15에 다섯 축이 후보 없이 닫히며 사실상 소진됐다 (`FAILED_THEORIES.md`,
  teammate-repo HANDOFF).
- 후처리 상수(SLOPE 1.0416 / SHIFT 0.0052)와 블렌드 가중(0.45/0.55)은 전부
  **학습기 표면(≤2024)에서 적합·외삽된 값**이다. 2025 예상 성공률 0.481도 외삽이다
  (teammate-repo `LEVERS.md:130`).
- 평가 시즌 2025는 학습 데이터에 대응물이 없는 두 가지 체제 변화를 갖는다:
  1. **ABS 존 상·하단 −0.6%p 하향 조정** (상단 56.35→55.75%, 하단 27.64→27.04%,
     신장 180cm 기준 약 1cm). 이 대회의 성공/실패 라벨은 존 기하의 함수이므로
     평가 시즌의 라벨 생성 규칙 자체가 학습 6개 시즌과 미세하게 다르다.
     출처: KBO 공식 공지(2025 시즌 초반 규정 변화 적용 경과), 남도일보 800877.
  2. **R리그 ABS 2년차.** 학습에는 R리그 신체제가 2024 한 시즌뿐이다.
- LB ≈ 로컬 편향제거 + 128.4 라는 큰 오프셋 자체가 평가 분포가 val2024와
  다르다는 실측 증거다.

## 2. 규정 근거

- 공식 Q&A 명시 허용: **"리더보드 점수에 기반한 모델·하이퍼파라미터·앙상블
  가중의 선택 및 보간"** (`README.md` Official Q&A Clarifications).
- 금지: 2025 트랙맨, 외부 2025 통계, 평가셋 행 간 정보, 평가셋 전체 기반 사후 보정.
  → **공개 LB 점수가 2025 체제를 관측할 유일한 합법 창구**다.
- 본 계획은 평가셋 "전체 기반 사후 보정"이 아니다: 각 제출은 행별 독립 추론이며,
  상수 선택을 LB 점수로 하는 것은 위 Q&A가 허용하는 하이퍼파라미터 보간이다.

## 3. 닫힌 축과의 구분 (재제안 아님의 근거)

| 닫힌 축 | 닫힌 이유 | 본 계획이 다른 점 |
|---|---|---|
| `calibration-refit-from-oof` CLOSED (−19.93) | 2023에서 적합한 보정을 2024로 외삽 → 보정 방향이 시즌 경계에서 반전 | 외삽이 아니라 **평가 분포 자체에서 측정**. 반전 기전이 성립하지 않음 |
| `isotonic`/월별 보정 CLOSED (−21/−52) | 동일 — 과거→미래 전이 실패 | 동일 |
| 후처리 예산 +13.1 상한 | val2024 잔여 캘리브레이션 예산 | 그 상한은 **val2024 기준**. 2025 분포의 편향은 별개 항이고, E-LB3(조건부)은 단조 변환이 아니라서 애초에 상한에 안 묶임 |
| `league-conditional-blend-weight` CLOSED | 2023↔2024 양방향 전이 실패 | E-LB4는 보류로 두고 E-LB1 결과 후에만 재론 (§5) |

## 4. 수학적 근거 — 점수는 프로브 상수의 정확한 2차식

Score = 100000·(1 − B/B₀), B = mean((pᵢ−yᵢ)²), B₀ = r(1−r) ≈ 0.25.

고정 방향 d에 대해 p(θ) = p₀ + θ·d 로 제출하면

```
B(θ) = B(0) + 2θ·mean(d·(p₀−y)) + θ²·mean(d²)
```

θ의 **정확한 2차식**(계수 3개)이다. 따라서:

- 현행 챔피언 점수(θ=0)와 대칭 프로브 2점(θ=±h)으로 포물선이 유일하게 결정된다.
- 꼭짓점과 기대 이득 (S = LB 점수):

```
θ* = −h·(S(+h) − S(−h)) / (2·(S(+h) + S(−h) − 2·S(0)))
기대 이득 = (S(+h) − S(−h))² / (−8·(S(+h) + S(−h) − 2·S(0)))
```

- 공개셋은 고정이므로 같은 제출은 같은 점수 → **측정 노이즈 0**. 유일한 위험은
  public→private 일반화인데, 수만~십만 행 규모에서 θ*의 표본 오차는 ~0.002,
  그로 인한 손실은 400000×0.002² ≈ 1~2점 수준. 반면 θ가 0.005 어긋나 있으면
  +10점, 0.01이면 +40점이 걸려 있다 (곡률 100000/B₀ ≈ 4×10⁵).
- 검증 장치: 세 점이 결정하는 포물선은 자유도가 없다. 만약 4번째 확인 제출이
  포물선을 벗어나면 채점이 결정적이지 않다는 뜻 → 전 계획 중단.
- 클리핑 안전: 블렌드 예측 범위 0.316~0.666 → |θ|≤0.02에서 [0,1] 위반 없음.

## 5. 실험 목록 (우선순위 순, 하루 1실험)

공통: 모든 arm은 검증 완료된 챔피언 zip(`b1s8_20260813.zip`) 기반으로
`script.py` 최종 출력 직전에 **상수 하나만** 추가한 변형이다. 제출 1회 변경 1개
원칙 유지. 각 arm의 zip sha256을 §7에 기록.

### E-LB1 — 전역 SHIFT 포물선 (제출 2회) ★ 최우선

- 방향: d = 1 (전 행 동일). `p_final = clip(p + δ, 0, 1)`.
- arm: δ = **+0.010**, **−0.010**. (δ=0 은 현행 1108.433.)
- 측정 대상: 2025 평가 분포의 평균 편향 mean(y−p) — 존 하향·ABS 2년차로 인한
  전역 이동을 직접 잰다.
- 채택: |δ*| ≤ 0.02 이고 기대 이득 ≥ +1 이면 δ* 반영본을 최종 후보로.
  |δ*| > 0.02 면 뭔가 이상한 것 → 반영하지 않고 원인 조사.

### E-LB2 — 블렌드 가중 w_cell 포물선 (제출 2회)

- 방향: p(w) = (1−w)·base + w·cell. 현행 w=0.55.
- arm: **w = 0.45**, **w = 0.65**.
- 주의: SLOPE/SHIFT·recent-middle 후처리가 블렌드 뒤에 오므로 w에 대해 엄밀한
  2차식은 아니고 **국소 2차 근사**다 (w 폭 0.2, 후처리는 매끄러운 변환이라
  근사 오차는 작다). E-LB1과 달리 근사임을 명시해 둔다.
- 채택: 기대 이득 ≥ +1, w* ∈ [0.40, 0.70].

### E-LB3 — 존 하향 조건부 방향 (제출 2회, E-LB1 완료 후)

- 동기: 존 −0.6%p 하향의 영향은 균일하지 않다. **구 존 상·하단 경계 부근에
  로케이션이 몰리는 투수**가 차등 영향을 받는다. 학습 시즌에는 존 이동이
  없으므로 이 계수는 오프라인으로 학습이 불가능하다 — LB만이 적합 수단.
- 방향 dᵢ: 투수 i의 트랙맨 2019–2024 로케이션 중 구 존 상단·하단 경계 밴드
  (경계 ±3cm)에 든 비중을 투수별 상수로 조인하고, 전 행 기준 평균 0/분산 1로
  표준화. 트랙맨 미링크 투수(행 커버리지 ~0.2%)는 0. 행별 독립 처리 유지 —
  규정상 허용된 asof-이전 집계다.
- arm: c = **+0.005**, **−0.005** (d 표준화 기준).
- 이 방향은 세그먼트 오프셋이라 단조 변환이 아니고, +13.1 캘리브레이션 상한에
  묶이지 않는다. 크기는 물론 **부호조차 사전 예측 불가** — 그래서 실측한다.
- 채택: 기대 이득 ≥ +2 아니면 폐기. |c*| ≤ 0.02 상한.
- 밴드 폭(±3cm)은 여기 고정한다. 결과를 보고 폭을 바꾸는 것은 방향 사후 선택
  (§6 금지)이다.

### E-LB4 — 리그별 SHIFT 분리 (보류)

F 행 비중이 낮아 곡률이 작고, 리그 조건부 축은 오프라인 전이가 이미 실패했다.
E-LB1의 δ*가 크게 나올 때만 "그 이동이 리그 공통인가"를 물을 가치가 생긴다.
그 전에는 실행하지 않는다.

## 6. 규율

- **방향 사후 선택 금지**: LB 점수를 보고 새 방향 d를 고르는 것은 선택 편의다.
  적합 대상은 본 문서에 사전등록된 방향(§5)뿐. 총 적합 자유도 ≤ 4.
- **오류 제출 방지**: 제출 오류도 일일 5회에 카운트된다. 매 arm은 제출 전에
  로컬에서 zip 구조·245,789행 추론·10분 시간 제한을 검증한다.
- **예산**: 남은 일수 16일 × 5회 = 80회. 본 계획 소요 6~8회 + 확인 1~2회.
- **셰이크업 보험**: 최종 선택 슬롯 중 하나는 무보정 B1S8을 유지한다. LB 적합
  결과 반영본은 나머지 슬롯에만 싣는다.
- **팀 공유**: 각 실험 종료 시 §7을 갱신하고 팀원에게 결과(포물선 계수·꼭짓점·
  기대 이득·채택 여부)를 공유한다. teammate-repo는 수정하지 않는다.

## 7. 결과 (append-only)

| 일자 | 실험 | arm | zip sha256 | LB 점수 | 비고 |
|---|---|---|---|---|---|
| (기준) | — | 챔피언 δ=0 | c2771fbd… (`b1s8_20260813.zip`) | **1108.4333490288** | 기준점 |
| | E-LB1 | δ=+0.010 | | | |
| | E-LB1 | δ=−0.010 | | | |

> 기준점 점수는 제출 이력에서 재확인 후 기입할 것. 포물선 계산은 §4의 식을
> 그대로 사용하고, 계산 스크립트와 함께 커밋한다.

## 8. 예상 시나리오

- **δ* ≈ 0**: 현행 SHIFT 외삽(0.481)이 이미 맞다는 확정. 제출 2회로 불확실성
  하나를 닫는 것 자체가 수확이다.
- **δ* 유의미**: 즉시 점수. 존 하향의 전역 성분이 실재한다는 뜻이므로 E-LB3의
  사전 확률도 올라간다.
- **포물선 부정합**: 공개 채점이 결정적이지 않다는 발견 → 전 계획 중단하고
  원인(공개/비공개 표본 방식) 조사. 이것도 그 자체로 중요한 정보다.



# GPT 제안 우선순위
## 결론

| 우선순위 | 실험 | 판단 |
|---:|---|---|
| 1 | **F 리그 partial-pooling logit adapter** | 현재 실행 가능한 가설 중 +30 규모를 수치로 설명할 수 있는 유일한 주력 후보 |
| 2 | **`GENERAL_SKILL_ADD` cell-only 재검증** | 저비용 보조 후보. 기대치는 +3 안팎이지 +30이 아님 |
| 3 | **RMSE/Brier 경로 수정 후 n=6 1회** | 원리적 근거만 남은 저기대값 확인 |

---

## 1. F 리그 partial-pooling adapter

### 왜 1순위인가

2024 미학습 구간에서 F 모델의 **행별 변별력**이 얇다.

| 진단 | F | R |
|---|---:|---:|
| Base honest normalized resolution | 507.8 | 839.3 |
| Cell honest normalized resolution | 429.8 | 861.8 |
| Base prediction SD | 0.0319 | 0.0454 |
| Cell prediction SD | 0.0327 | 0.0467 |

F는 오차가 폭발하는 것이 아니라, 예측을 평균 근처로 좁혀 순위 구분을 포기하는 형태다. F는 2024 검증의 `30,010 / 253,507 = 11.84%`다.

R 수준까지 F resolution 격차를 회복한다고 가정한 **진단상 상한**:

- Base: `(839.3 - 507.8) × 0.1184 × 0.994 ≈ +39.0 BSS`
- Cell: `(861.8 - 429.8) × 0.1184 × 0.994 ≈ +50.8 BSS`

이는 예상 이득이 아니라 이론적 여지다. 그래도 현재 저장소에서 +30을 산술적으로 설명할 수 있는 가장 큰 미회수 영역이다.

### 첫 구조

```text
R: p_new = p0
F: p_new = sigmoid(logit(p0) + g_F(x))
```

- `p0`: B1S8 최종 예측
- `g_F`: 강한 규제의 Ridge 또는 depth 2 수준 얕은 모델
- 첫 입력: `logit(p0)`, pitcher ball/middle rate와 season-standardized 또는 delta 표현, ball-strike count
- 목적: F 평균 이동이 아니라 **F 내부 순위와 분산 회복**
- R 예측은 완전히 고정

### 검증 방식과 게이트

- F 2023 honest prediction/잔차로 adapter 학습
- 사양 고정 후 F 2024에 동결 적용
- 2024 타깃을 보고 feature·계수·weight 재선택 금지

확장 조건:

- R prediction 최대 변화 `0`
- 전체 raw BSS `≥ +10`
- F normalized resolution `≥ +100`
- 2024 전반기·후반기 모두 양수

직접 제출 후보는 전체 `+25~+30`과 반기 안정성이 확인될 때만 인정한다.

### 반복 금지

- F-only CatBoost: 신체제 55,696행, TE 결측 71.5%로 실패
- F별 base/cell weight: `2023→2024 +1.484`, 역방향 `-2.119`
- F 상수 shift/calibration: 순위를 바꾸지 못함
- submission에서 old F 제거: 과거 약 52점 약한 모델을 정상 재현으로 오판
- league-relative normalization 재포장: 기존 실패 축과 중복

---

## 2. `GENERAL_SKILL_ADD` cell-only

기존 fresh-control 6-seed:

- Base: `-0.077`
- Cell: `+2.568`, `t=2.18`, `6/6 positive`
- Core: `+1.104`, 95% CI `[-1.69, +3.90]`

cell-only는 결과를 본 뒤 정의되는 새 가설이므로 별도 사전등록이 필요하다. 구현비용은 낮지만 **+30 후보가 아니라 +3 전후의 보조 후보**다. feature subset·계수·blend sweep은 하지 않는다.

---

## 3. RMSE/Brier 경로

- 과거 기록: `+2.44`, `t=0.80`, `n=3` — 판정 불충분
- 코드 감사: RMSE refit frame이 일반 경로와 다르고, `CatBoostRegressor`가 현재 `fpipe.predict()`의 `predict_proba` 경로와 호환되지 않음
- 학습·refit·저장·추론 parity를 먼저 고친 뒤 n=6 한 번만 실행
- 현재 근거상 +30과 거리가 멀어 3순위

---

## 종료/중단

| 축 | 근거 |
|---|---|
| P3-C/P3-C2 class weighting | P3-C2 FAIL; 사전등록상 전체 family 종료 |
| calibration/isotonic/slope 재탐색 | 완벽한 recalibration 상한 약 `+13.1~+13.3`; transfer는 악화 |
| 대규모 Optuna/HPO | holdout 6-seed `-7.44` |
| segment residual/offset | 같은 시즌 양수 후 다음 시즌 역전 반복 |
| ranking | fixed-core `-219.73`, resolution 급락 |
| FM multilabel | single-seed core `-12.07` |
| predicted pitch-type 기존 경로 | oracle `+120~+142`, 합법적 주변화 `-13~-29`, masked auxiliary LB 약 `-2.3~-2.5` |
| 기존 TabM/TabDecoder | 챔피언 블렌드에서 유효 가중치 없음 |
