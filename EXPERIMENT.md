# EXPERIMENT.md — 현재 상태판

> 지금 무엇이 최선이고, 무엇이 열려 있고, 다음에 뭘 재는가.
> **역사는 여기 없다** → [docs/EXPERIMENTS_LOG.md](docs/EXPERIMENTS_LOG.md)
> 닫힌 질문 → [docs/SETTLED.md](docs/SETTLED.md) · 실행 기록 → `LEDGER.tsv`

갱신 2026-08-09 (RT1 roster-transition·PG1 pitcher×league 실행 중)

## Objective

Brier Skill Score 최대화. 목표 **1120대 합법 진입**. 현재 1위 1,198.02.

---

# Current Best

| 제출 | 구성 | 편향제거 로컬 | LB | 순위 |
|---|---|---|---|---|
| v14 | 셀 depth8 w0.20 | — | 1,083.52 | |
| v15 | 셀 depth5(ZD5) w0.38 | 963.88 | 1,092.01 | 7위 |
| **v18** | **w 0.55** | **965.21** | **1,093.85** | **11위** |
| v16 | SHIFT 0 (폐기) | — | 1,085.86 | |

**제출 파일: `submissions/blendv9_0808_0126.zip`**

현재 순위표 갱신 뒤 v18은 11위다. 점수와 제출 파일은 변하지 않았다.

### 2026-08-09 신규 직교 모델 탐색

| 후보 | 미학습 2024 편향제거 | AB_base와 rms | AB 단독 대비 최적 이득 | 상태 |
|---|---:|---:|---:|---|
| MC1 확률일관성 MLP | 6시드 701.99 | .0256 | 현행 위 +1.36 | 전이 한쪽 가중 0, 종료 |
| TM1 현재 121피처 TabM | 821.81 | .0121 | 0.00 | Cat과 너무 유사, 종료 |
| TMC1 TabM+실패셀 marginal | 6시드 839.34 | .0126 | 현행 위 0.00 | 파일럿 선택편의, 종료 |
| SK2 std-k 40 | 8시드 910.49 | — | 현행 위 절반교체 +0.19 | 후반기 음수, 종료 |

TMC1 파일럿의 `+16.58`은 6시드에서 사라졌다. 최종 `AB_base+DW_cell+TMC1` 가중은
`.354/.646/.000`, 점수는 기존 두 멤버와 같은 902.63이다. 전반→후반에서도 TMC
가중 0이었다. MC1은 현행 위 +1.36뿐이고 반대 반분에서 가중 0이라 제출하지 않는다.

`std-k 40`도 실제 제출 표면(val2024, drop 없음, 4070 8시드)에서 닫혔다.
`SK2_k40−VB2_base = -1.954`(SE 2.377, t=-.822), 현행 블렌드 base 완전교체
-0.564, 절반교체 +0.194이며 절반교체 후반기 효과가 -0.305라 제출하지 않는다.

### Best Configuration

```text
멤버      cat_v14f × 8시드 (이진)  +  cat_ZD5 × 6시드 (실패모드 셀 다중분류 depth5)
가중      base 0.45 / cell 0.55
후처리    SLOPE 1.0416 (로짓 기울기) → SHIFT 0.0052 (상수 차감)
피처      --feat-v2 --te p,pc,ph,b,pi --te-dev --feat-std --std-k 80
          --std-to-prior --std-season-prior --feat-domain --feat-skill-pc
학습      --lr 0.01 --es 500 --depth 8 --l2 10 --refit-mult 1.5
추론      33.7초 / 600초 (6 vCPU, 245,789행)
```

### LB 환산 (중요)

**`LB ≈ 편향제거 로컬 점수 + 128.4`**

원점수 기준 오프셋(153→155→157)은 가중을 바꾸면 밀린다 — 원점수가 수준 변화를
벌점으로 먹는데 SHIFT 가 그걸 되돌리기 때문이다. 편향제거 기준은 128.13→128.64 로 안정.

---

# 남은 여지 — 실측

미학습 2024 표면에서 Brier 를 분해했다(20분위):

```
신뢰도 (후처리로 고칠 수 있음)   +13.1     ← SLOPE 가 이미 +3 을 먹음
해상도 (모델 실력)              +882.8
```

**후처리로 남은 건 10점 미만.** 점수를 올리려면 모델이 더 잘 **변별**해야 한다.

평가 구조의 비용:

```
랜덤 4-fold CV (같은 시즌 안 보간)   2076
미학습 다음 시즌 예측                 880
                              차이  1196
```

상위권과의 32점 차이는 이 1196 안에서 벌어지는 다툼이다.

---

# 열려 있는 것

| 축 | 상태 | 비고 |
|---|---|---|
| std-k 80 → 40 | **CLOSED** | 제출 표면 페어 -1.95(t=-.82), 절반교체 +0.19·후반기 음수 |
| 새 출력 기하 멤버 | 현재 큐 소진 | MLP·TabM·셀 일관성 모두 현행 블렌드 가중 0 또는 미미 |

**증류는 닫혔다** (아래 Decision Log · `docs/SETTLED.md --soft-target`).

# 최근에 닫힌 것 (2026-08-07~08)

기각: Optuna · GPBoost · trackman · 세그먼트 드리프트 · LightGBM · 피처 부분공간 ·
refit 배수 2.0(18시드) · te-k 축별 수축 · **셀 × 카운트 맥락** · **2스트라이크 라우팅** ·
F리그 분리 · isotonic 계열 전반 · 시드 확장

소진: 셀 기하(두 번째 멤버 +0.34, d4·c16 가중 0)

---

# Current Hypothesis

새 1순위는 `RT1 roster-transition`이다. F→R 투수는 2020~2024 매년 R 평균보다
−.0110~−.0338 낮았고, 2024 VB2가 이 구간을 .01608 과대예측했다. 2023R에서 고정한
조건효과는 2024R 편향제거 기준 +4.09로 전이했다. generic 복귀·타자 이동은 불안정하다.

기존 `F 2023=ABS 1년차` 해석은 공식 연혁과 실패모드 감사로 폐기했다. 퓨처스 ABS는
2020년부터 운영됐고 F reverse가 2022 .0497→2023 .2988로 뛴 것은 데이터/타깃 체제
단절이다. 현행 v18 SHIFT 값은 새 검증 없이 바꾸지 않는다.

H1 dynamic hierarchy는 source 2022→2023R +8.03이 target 2024 -19.09로 반전해 닫혔다.
OR1 segmentation fault는 PairLogitPairwise group64의 pair 메모리 폭증으로 확인했다.
group16은 50iter smoke를 통과해 A100 정식 `OR2_rank16`을 실행 중이다. 4070의
`OR2_rank16p`는 refit 중 Windows native access violation로 끝나 A100 결과만 쓴다.
baseline-col의 refit/test/제출 누락은 수정했고 4070 `BC1_offset`으로 재검정한다.

신규 모델 파일럿은 모두 음수로 종료했다. MLP-PLR은 미학습 최고 641.29였고
sigma1 517.60, sigma10 426.58로 악화했다. content two-tower도 미학습 675.32 /
제출 표면 699.67에 그쳐 두 축 모두 6시드 확장 없이 닫았다.

G0 topology-only graph는 A100 미학습 2024 `866.90`(동일 seed AB 대비 -1.93),
4070 제출 표면 `902.06`(동일 seed VB2 대비 -9.16)으로 종료했다. 두 표면 모두
음수라 G1 GraphSAGE 승격 없이 그래프 축을 닫았다.

> **Claude 최종 판정: 증류 기각.** 판정 표면 `+38.43` 의 85% 가 교사만 본 F 구체제
> 행이었고(`DT5_seq` 로 +4.6~7.0 붕괴), 제출 표면에서는 base 대비 −10.19 에 블렌드
> 가중 0.00 이다. base 와 rms 0.0067 로 셋 중 가장 닮았다 — 새 기하가 아니라 base 의
> 매끄러운 복사본이었다.
>
> 후처리·하이퍼·증류·`std-k 40`까지 닫혔다. 현재 검증 큐에는 목표 1105까지
> 필요한 +11을 낼 근거 있는 축이 없다. 현행 v18을 보존하고 새 직교 가설이 필요하다.

# Next Experiment

## 2026-08-09 04:37 야간 분리 실행

- A100: `RT1A_roster`(미학습 2024) → 정상 종료 시 `PG1A_pg` 순차 실행.
- 4070: `RT1V_roster`(val2024 제출 표면) → 정상 종료 시 `PG1V_pg` 순차 실행.
- RT1은 타깃 없이 시즌 S 이전 투수의 직전 등장 리그·공백과 R/F 이력량만 쓴다.
  테스트 행끼리 상태를 갱신하지 않으며 cutoff/행 독립 검문을 양 머신에서 통과했다.
- PG1은 이미 구현돼 있으나 현행에서 빠진 `pitcher_id×game_type` expanding TE를
  단독 변경으로 더한다. 둘 다 단일시드 게이트이며 자동 다중시드 확장은 하지 않는다.
- 로그: A100 `out/rt_pg_a100.log`, 4070 `out/rt_pg_4070.log`.

## Codex 단독 실행 큐 (임시 운영)

0. **O1 기각:** `OG1_cell14` 6시드. 2023 R 반분 source에서는 cells가 scalar보다
   최소 +101.32였으나 미학습 2024 R route에서 −52.38. posterior 잔차가 시즌 비전이.
1. **OR2 종료:** group64는 pair 전개 메모리 폭증, group16 full refit도 4070과
   A100 양쪽에서 native crash. no-refit smoke만 통과했으며 산출물 없는 현재 ranking
   구현은 성능 비교에 쓰지 않고 보류한다.
2. **4070 완료:** `BC1_offset=899.31`, 동일 seed `VB2_base=911.22` 대비 −11.91.
   수정된 baseline 경로에서도 개선되지 않아 확장 없이 닫는다.
3. **다음 1순위:** `RT1 roster-transition`. 투수 직전 리그와 같은/다른 리그 이력량을
   시즌 이전 lookup으로 만들고 4070 val2024 seed42 한 변경 게이트부터 수행한다.

1. **완료:** `TH2_hl2`, 4070 val2024 8시드, drop 없음. 지문 일치(exit 0),
   오류 없음. `VB2_base` 대비 paired +0.164(SE 1.760, t=.093)으로 승격 실패.
2. **4070 / 완료:** 기존 예측만으로 logit blend·세그먼트 resolution·연도 전이 검문.
   logit `+0.021`, 타자경험 `-47.618`, 투수×타자손(k=100) `-191.952`로 모두 GPU 승격 실패.
3. `TW1_window`는 앙상블 +3.75, 페어 +3.42(SE 2.40, t=1.42), blend +3.78로
   보류. 추가시드보다 `TA1_anchor` 신규 축을 우선한다.
4. `TA1_anchor`는 앙상블 +1.07, 페어 -0.58(SE 2.40, t=-.24), blend +2.06으로
   약해 후순위 보류. 다음 신규 GPU 축으로 승격하지 않는다.
5. RMSE, 기존 TabM 단순 재블렌드, 현재 형태의 skill-hand는 닫힌/중복 축이라 재실행하지 않는다.
6. `TH1_hl2`는 A100 판정 표면에서 앙상블 +7.15, paired +6.03(SE 1.82,
   t=3.32), blend +7.29로 채택. `TH2_hl2`가 제출 표면 전이 여부를 결정한다.
7. `TH2_hl2` 고정 대체는 현행 블렌드 대비 +0.610뿐이며 양방향 시즌 반분에서
   각각 -1.573/-1.112. 새 제출 zip을 만들지 않고 현행 v18을 유지한다.
8. 현행 `blendv9_0808_0126.zip` 재검증 완료: 행 독립 최대차이 0, 모델 14개,
   5행 2.4초, 평가규모 245,789행 29초(한도 600초), 종합검증 exit 0.
9. `SK2_k40` 4070 제출 표면 8시드 완료: 지문 일치(exit 0), 페어 -1.954
   (SE 2.377, t=-.822), 현행 블렌드 완전교체 -0.564, 절반교체 +0.194.
   절반교체 전반기 +0.417/후반기 -0.305로 제출 승격 실패. GPU 양쪽 유휴.

Claude 검토는 현재 진행 조건이 아니다. 사용자가 복귀를 지시하기 전까지 Codex가 수치와
규칙을 근거로 다음 큐까지 결정한다.

## Claude 검토 → P3 방향 확정

### Goal

Codex 실행은 완료됐다. `HANDOFF.md`의 수치·로그·지문을 독립 검토해 다음을 정한다:

- `DX2_seq` 최종 기각 및 P3 가중 후보 제외 여부
- DT5 경계값(t=2.36) 추가 시드의 가치 vs 증류 축 종료
- 다음 우선순위를 새 피처·손실·블렌드 다양성 중 어디에 둘지

### Keep Fixed

```text
P2'   4070 / val2024 / 8시드 / 학습집합 지문 0.5401750413
P2'-C A100 / val2023→test2024 / 6시드 / 양쪽 --drop-f-pre 2022
AB2_base·DT4_seq(P2'-B)는 구조적 무효이므로 비교 금지
```

### Success Criteria

```text
Codex 1차 분석은 잠정이며 SETTLED 추가·제출 결정은 하지 않는다.
Claude가 코드·LEDGER·동일 머신/표면 조건을 확인한 뒤 최종 판정한다.
```

---

# Decision Log

## 2026-08-09 관계형 신규 축

상세 설계는 [`docs/HIERARCHY_GNN_PLAN.md`](docs/HIERARCHY_GNN_PLAN.md)에 고정했다.
2024 과거 그래프 감사에서 exact-pair hit 51.2%, 투수 hit 80.1%, 활성 투수 이웃 Jaccard
중앙값 0.360을 확인했다. 첫 실험은 full GNN이 아니라 동결 topology-only graph feature의
미학습 2024 gate이며, 통과할 때만 inductive GraphSAGE로 확장한다. 테스트 행 사이 message
passing 또는 temporal state 갱신은 행 독립 위반으로 금지한다.

| 날짜 | 결정 |
|---|---|
| 08-07 | 오프셋의 정체는 SHIFT 가 아니라 **구조적 이득**(한 시즌 더 학습) |
| 08-08 | 비교군은 **같은 머신**에서 만든다 (11점 차 실측) |
| 08-08 | LB 환산 좌표를 원점수 → **편향제거**로 교정 |
| 08-08 | 손실 지도의 F리그 구멍은 **착시** (MSE 로는 R 보다 낮음) |
| 08-08 | 후처리 여지 총 +13.1 확인 → 상수 튜닝 종료 |
| 08-08 | **증류 기각.** 판정 표면 이득의 85% 가 교사/학생 데이터 비대칭. 제출 표면 −10.19, 가중 0.00 |
| 08-08 | 판정 표면(val2023)은 `--drop-f-pre 2022` 를 **요구**하고 제출 표면(val2024)은 **안 쓴다**. 명령을 표면 간 복사 금지 |
