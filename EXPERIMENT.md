# EXPERIMENT.md — 현재 상태판

> 지금 무엇이 최선이고, 무엇이 열려 있고, 다음에 뭘 재는가.
> **역사는 여기 없다** → [docs/EXPERIMENTS_LOG.md](docs/EXPERIMENTS_LOG.md)
> 닫힌 질문 → [docs/SETTLED.md](docs/SETTLED.md) · 실행 기록 → `LEDGER.tsv`

갱신 2026-08-13 (**B1S8이 LB 1108.4333490288로 현행 챔피언, 순위 #34**)

## Objective

Brier Skill Score 최대화. 1차 목표 **1120대 합법 진입**. 사용자 제공 리더보드에서
최근 확인한 최고점은 1,288.180881이며 실시간 현재값은 별도 확인 전이다.

## 2026-08-15 P3-C2 종료 — 제출 후보 없음, 챔피언 불변

Success cell 9·10만 CE 질량을 균형화하고 residual cell 11은 weight 1로
유지한 P3-C2를 fresh control과 같은 5070 세션에서 seed 3으로 판정했다.
추론 success 집합 `[9,10,11]`, taxonomy, base, 0.45/0.55 blend는 고정했다.

```
cell       880.957 -> 878.993   delta -1.965
fixed core 888.772 -> 887.550   delta -1.222   => FAIL
source 2023 core delta -6.671
first +8.346 / second -10.790 / R -0.078 / F -9.748
```

가중치와 analytic deweight는 계약대로 작동했지만 resolution이
`0.00222055 -> 0.00221857`로 감소했다. 후반과 F에서 크게 악화됐고,
seed-3 고정코어 delta가 0 이하이므로 사전등록대로 6시드 확장 없이
P3-C/P3-C2 class-weight 계열 전체를 닫는다. 챔피언 14멤버는 bit-identical,
두 artifact의 새 프로세스 예측 및 subset/reversal/half/single-row drift는 0이다.

## 2026-08-15 앞선 축 종료 — 제출 후보 없음, 챔피언 불변

| 축 | 판정 | 고정코어 delta | GPU |
|---|---|---|---|
| RANK16 (PairLogitPairwise base 슬롯) | **FAIL** | −219.73 (미학습 2024) | 사용 |
| H1ADD base-only | **DROP** | +0.387, 95% 상한 +2.61 | 0 |
| D12 (fixed-core checkpoint selector) | **FAIL** | −62.96 (미학습 2024) | 사용 |
| P3-A (base 8시드 trimmed 평균) | **CLOSED** | −0.23 | 0 |

세 건이 GPU를 쓰지 않았다. 공통 기전은 **resolution이 안 늘어난다**는 것이다.
H1ADD는 대조군과 pearson +0.997223이라 core resolution이 0.002386→0.002389,
P3-A는 8시드가 RMS 0.000464로 이미 일치해 극단값을 버릴 여지가 없다. D12는
선택 시즌에서 +12.16을 찾았지만 그 지점이 **넓은 최적점이 아니라 좁은 스파이크**
(400에서 620.57, 600에서 587.69)라 미학습 시즌으로 전혀 넘어가지 않았고,
`refit-mult 1.5`가 그 스파이크를 1.5배 해서 골짜기에 착륙시켰다 —
**뾰족한 체크포인트와 refit 배수는 함께 쓸 수 없다.**

후속 P3-B는 +0.278로 DROP, P3-C는 안전 게이트에서 HOLD, P3-C2는 위와 같이
FAIL했다. 현재 사전등록된 GPU 후속 축은 없다.

## 2026-08-15 RANK16 — 성능 FAIL, 축 종료 (제출 없음)

PairLogitPairwise를 base 슬롯에 넣는 축을 사고 복구 후 처음으로 **측정**했다.
판정 표면·seed 3·동일 host(DESKTOP-053T952), 세 멤버의 `row_id`와 타깃이
원소 단위로 동일하다.

```
base 865.50   cell 881.02   rank 28.73          (미학습 2024 raw BSS)
CONTROL_CORE 888.81   RANK_CORE 669.08   delta -219.73
rank resolution 0.000364  vs  base 0.002174
```

기전: 목적함수가 확률을 보지 않으므로 척도 전체가 사후 시그모이드
(`b=0.0958`) 하나에서 나오고, 모든 점수가 기저율 주변으로 눌린다. Murphy 분해가
이미 말한 대로 **캘리브레이션 여지는 최대 +13.3, 나머지는 전부 resolution**이라
캘리브레이션을 내주는 목적함수는 여기서 이길 수 없다. stage 2·25 미실행,
6시드 확장 취소, 캘리브레이션·group size 추가 탐색 없음.

부수: RANK16 **production stage-1 경로**에서 GPU 조기종료 산출물이 3/3으로
손상·종료불가였고, **fixed-length 2-pass가 안전한 우회로**로 확인됐다(stage 12
1/1 정상). 과거 ES 경로의 정상 대조군이 있으므로 라이브러리 일반 성질로
확대하지 않는다. 상세는 `docs/SETTLED.md`의 `RANK16 closure audit`.

## 2026-08-13 현재 상태 — 감사 후 재빌드 두 건 제출, 둘 다 개선

```
v11    1101.8020672065   (2026-08-09, 이 아래 항목들의 기준선)
B1S6   1107.5460442737   +5.744   P0+P1 재빌드           #38 -> #34
B1S8   1108.4333490288   +0.887   base 시드 6 -> 8       누적 +6.631
```

이 아래의 2026-08-12 이전 항목은 **모두 v11 기준으로 쓰였다.** 절대 점수를 지금
챔피언과 맞대지 말 것. 오늘 닫힌 축·미해결 결정·다음 단계는
[HANDOFF.md](HANDOFF.md)에 있고, 기전은 `docs/SETTLED.md`에 있다.

로컬→LB 환산은 **편향제거 점수 + 139.03** (B1S 계열 한정, 두 점에서 확인).
옛 계열의 +128.4를 그대로 쓰면 부호까지 틀린다.

## 2026-08-12 SR1·MDU1 — 제출 후보 없음, 소스 정합성 복구

- 강한 K0의 honest residual을 raw 공식 피처로 학습한 SR1은 `best_iter=0`; 고정 2%
  결합이 source 2023 `−0.038`, strong 2024 `−0.014`였다.
- base/cell disagreement q-bin 보정 MDU1은 signed arm이 `−2.349/−2.106`, abs arm도
  source `−0.303` 및 strong R `−1.747`로 이전되지 않았다. 두 축 모두 CLOSED.
- 실제 v11 제출 ZIP은 recent-middle+PB로 정상이다. 다만 `src/script_blend_v11.py`가
  v12 career-middle 내용으로 남아 있던 인프라 위험을 발견해, 실제 ZIP과 동일한 v11로
  복구하고 career 버전을 `src/script_blend_v12.py`로 분리했다. LB 해석에는 영향 없음.
- PB 저랭크 수축은 사전검문에서 이미 CLOSED(`−2.639/+0.350/+0.551`)임을 재확인해
  중복 실행하지 않는다. 다음 신규 검정은 base/cell 간 차이와 구별되는 **동일 base 계열의
  시드 간 예측분산**이었다. EV1 q8은 `−0.966`, 구조적 수축은 최고 전체 `+1.369`이나
  전반 `+4.255` / 후반 `−2.401`로 반전해 CLOSED. 신규 제출 후보는 없다.
- 이어서 코어 학습의 미시도 축인 Bernoulli bootstrap 0.8을 단일 변경했다. BTP1 단독은
  MVA 대비 `−13.63/−14.19`; K0 base 10% 교체도 `−0.159/−0.107`이었다. 기본 Bayesian
  bootstrap을 유지하며 subsample 추가 탐색은 하지 않는다.

## 2026-08-12 core 보존형 신규 탐색 — 제출 후보 없음

K0(recent-middle+exact-PB)를 보존한 약신호 salvage, FT-Transformer, BrierScore
early-stop, 현재 121피처 XGBoost를 로컬 RTX 5060에서 순차 검증했다. FT는 unseen
`712.940`, Brier early-stop은 기준과 같은 best_iter 826/점수 차 −0.03으로 종료했다.

XGBoost 10%는 약한 local single-seed K0에서 세 전이 `+2.640/+20.294/+3.749`로
유망했고 최신 6시드도 개별 `+3.092~+4.157`이었다. 그러나 실제 챔피언에 가까운
4070 VB2×8+ZD5×6 아날로그에서는 2/5/10%가 `−0.053/−0.369/−1.526`으로 사라졌다.
refit×1.5 개선본도 최선 2% `+0.094`(R/late 음수)라 제출하지 않는다. 현행 v11을 유지한다.
상세: [`docs/RECORD_AUDIT_20260812.md`](docs/RECORD_AUDIT_20260812.md).

## 2026-08-12 HFC1 실패모드 계층 분해 — 1시드 gate 기각

flat 14-cell과 같은 NPZ·로컬 GPU·seed42에서 실패모드 joint를 네 조건부 이진 모델로
분해하고 테스트 모드 없이 8상태를 주변화했다. 2023 검증은 `591.982 vs 592.131`
(`−0.149`)로 같았지만 미학습 2024는 `514.976 vs 586.631`(`−71.655`)로 반전했다.
25% 고정 블렌드도 `+15.135→−2.519`, centered test 차이도 `−71.751`이다. 출력 다양성은
RMS `.0143`으로 충분하나 조건부 calibration이 시즌을 넘지 못했다. 다중시드 없이 CLOSED.
상세: [`docs/RECORD_AUDIT_20260812.md`](docs/RECORD_AUDIT_20260812.md).

## 2026-08-12 로컬 정식 core·예측 구종확률 파생 감사

동일 seed42 정식 depth5 cell을 재생성했다. base `876.899`, cell `890.63`, 고정
`.45/.55` core는 미학습 2024 `900.175`다. 이 core 위 59개 전수 블렌드에서 모든
CatBoost 파생 후보는 2024 음수였고 PMT1 6시드 평균도 10% `+2.793`으로 +3 미달이다.

행단독 예측 구종분포의 entropy/max/margin과 3개 확률, 2차원 q4 조합을 2023 반분→2024로
검문했다. 최선 `fastball×breaking`은 `+2.797/+2.602`, 단일 offspeed는
`+1.376/+3.212`라 두 표면 +3을 동시에 넘는 후보가 없다. 실제 구종 주변화·masked MLP에
이어 예측 구종확률 파생피처도 CLOSED. 상세:
[`docs/RECORD_AUDIT_20260812.md`](docs/RECORD_AUDIT_20260812.md).

챔피언 cell의 3000 iter 상한도 단일 변경으로 감사했다. 5000 허용 시 best_iter는
3928로 이동해 2023이 +1.63 올랐지만 미학습 2024 cell은 `890.63→881.96`(−8.67),
core 전량 치환은 −2.688이었다. 10% 치환부터 2024가 음수라 기존 3000 iter를 유지한다.

## 2026-08-11~12 masked 구종 보조학습 — v13·v14 모두 LB 실패, 축 종료

예측 구종을 제구 확률에 직접 주변화하는 경로는 계속 CLOSED다. 대신 strict 1:1로 복원한
현재 구종을 과거행의 masked auxiliary label로만 쓰고, 추론에서는 제구 head만 출력하는
공유 MLP가 새로 통과했다. 제구 단독망 대비 6시드 paired 평균은 2021→22/2022→23/
2023→24 각각 `+89.858/+72.361/+18.432`, t=`7.882/4.980/4.083`이다.

v13 full-fit 제출은 `1099.4652219091`, v11 대비 `−2.336845`였다. 원래 rolling은
전처리 artifact/QT를 모델보다 한 시즌 더 과거에 동결했지만 v13은 전처리까지 2024에
full-fit했다. 이 제출형 구조를 과거에 재현하자 보조 증분이 2022→23 `−198.662`
(t=−3.758), 2023→24 `−123.688`(t=−6.348)로 반전했다. old/new 후처리는 거의 동일해
실패 원인은 NN 전처리 refit이다.

원래 통과한 구조를 평행 이동한 `전처리≤2023, 모델≤2024, 예측2025`
`v14_pitch_mtl_lag`도 LB `1099.2989837237`(42초)로 실패했다. v11 대비
`−2.5030834828`, v13 대비 `−0.1662381854`다. 가중치 10%, slope/shift,
recent-middle/PB는 불변이고 패키지·smoke·행 독립 감사도 통과했으므로 실행 오류가 아니다.
두 cutoff가 사실상 같은 LB 손실을 낸 결과, masked 구종 auxiliary의 rolling 양수는
2024→25에 일반화되지 않았다. 구종 보조학습의 가중치·gate·refit 추가 탐색은 종료하며
현재 챔피언은 계속 v11이다. 상세:
[`docs/PITCH_MTL_LB_FAILURE_20260811.md`](docs/PITCH_MTL_LB_FAILURE_20260811.md).

## 2026-08-11 SBS 다음 구종·멀티태스크 gate

선수 map2와 행 단독 상황키로 main:Trackman 현재구종 1:1 라벨을 전체 75.1668%,
2023/2024 77.1225%/76.5947% 복원했다. 실제 구종 oracle은 로컬 기준선 대비
`+120.693`, 구종×count는 `+141.909`로 강했지만, 미학습 2024에 합법적으로 예측한
구종확률로 주변화하면 `−13.043~−28.871`이었다. 대회 47열 전부를 쓴 구종 head도
정확도 53.606%, 기대보정과 실제 제구잔차 상관 ≤.0017이다.

따라서 SBS식 직전구종 sequence는 행독립상 제외하고, predicted-pitch marginalization은
CLOSED다. 다만 구종을 최종 확률에 직접 넣지 않는 masked auxiliary representation은
별도 실험으로 재개해 위 제출 후보까지 승격했다.
상세: [`docs/SBS_PITCH_MULTITASK_RESEARCH_20260811.md`](docs/SBS_PITCH_MULTITASK_RESEARCH_20260811.md).

## 2026-08-11 로컬 피처 엔지니어링 후속

평가 서버와 Python/pandas/numpy/sklearn/joblib 버전을 맞춘 노트북 RTX 5060에서
`MVA_native=876.90`을 고정 기준으로 9개 단일변경을 실행했다. 소규모 피처 삭제는
`-4.99~-45.21`, recent-pair q-bin/TE는 `-35.63/-7.84`, exact-PB 모델 TE는
`-17.56`, count 12상태 범주는 `-9.57`, 투수·타자 quality-min은 `-10.95`였다.
다중시드로 승격할 후보와 새 제출 패키지는 없다.

과거 타석 깊이 proxy, 구종/실패 entropy, 홈·원정 합성도 CPU 두 전이 게이트를
통과하지 못했다. 상세와 재현 경로는
[`docs/LOCAL_FEATURE_AUDIT_20260811.md`](docs/LOCAL_FEATURE_AUDIT_20260811.md)에 있다.
현행 v11/LB 1101.802를 유지한다.

## 2026-08-11 로컬 전수 데이터 감사

원격 없이 로컬 RTX 5060에서 공개 train 1,475,092행·공식 입력 47개와 Trackman
1,793,078행·30개 컬럼을 전수 조사했다. 단변수 전체, 2024의 1,081개 모든 쌍,
상위 235쌍의 3개 rolling 전이, 안정 쌍의 선택적 3중 조합, 현행 모델 잔차의 모든
단/쌍 관계를 확인했다. 상세는 [`docs/FULL_DATA_AUDIT_20260811.md`](docs/FULL_DATA_AUDIT_20260811.md).

- 결측: native NaN 876.90. 0/-1/평균/중앙값/계층 fallback/indicator는 seed42에서
  모두 `-6.58~-12.07`; indicator 추가 seed 3/4/5 평균도 `-1.665`.
- 잔차: 두 전이의 전체·전반·후반·R·F를 모두 통과한 쌍은
  `pitcher_id×batter_id`뿐(`+1.874/+4.595`)이며 이미 v11 PB다.
- 안정 recent×career 쌍을 명시적 피처로 만들면 약 `-9.3`; league×runner `-11.91`.
- CTR complexity3은 seed42 `+3.338`이나 추가 3시드 평균 `+0.791`, t=.419,
  앙상블 `+0.471`로 승격 실패.
- Trackman 전년도 투수 요약 21개는 약 77% 커버지만 모든 단변수의 최소 raw BSS 음수.

**새 제출 후보 없음. 현행 v11과 결측 처리(native raw + 파생 prior/n0)를 유지한다.**

---

# Current Best

| 제출 | 구성 | 편향제거 로컬 | LB | 순위 |
|---|---|---|---|---|
| v14 | 셀 depth8 w0.20 | — | 1,083.52 | |
| v15 | 셀 depth5(ZD5) w0.38 | 963.88 | 1,092.01 | 7위 |
| v18 | w 0.55 | 965.21 | 1,093.85 | |
| v16 | SHIFT 0 (폐기) | — | 1,085.86 | |
| v10 | v18 + recent-middle q8/k500 | — | 1,100.089 | |
| **v11** | **v10 + pitcher×batter residual k500** | — | **1,101.802** | |
| v12 | career-middle q8/k200로 교체 + PB 재적합 | — | 1,083.531 | 폐기 |

**현행 챔피언 제출 파일: `submissions/v11_pb_posix_0809.zip`**

v12는 오프라인의 상대 전이 이득 `+3.174`와 달리 LB에서 v11 대비 `−18.271`로
반전했다. PB 테이블은 v11과 상관 `.999176`이라 손실축은 career-middle 교체다.

### 2026-08-09 신규 직교 모델 탐색

| 후보 | 미학습 2024 편향제거 | AB_base와 rms | AB 단독 대비 최적 이득 | 상태 |
|---|---:|---:|---:|---|
| MC1 확률일관성 MLP | 6시드 701.99 | .0256 | 현행 위 +1.36 | 전이 한쪽 가중 0, 종료 |
| TM1 현재 121피처 TabM | 821.81 | .0121 | 0.00 | Cat과 너무 유사, 종료 |
| TMC1 TabM+실패셀 marginal | 6시드 839.34 | .0126 | 현행 위 0.00 | 파일럿 선택편의, 종료 |
| SK2 std-k 40 | 8시드 910.49 | — | 현행 위 절반교체 +0.19 | 후반기 음수, 종료 |

TMC1 파일럿의 `+16.58`은 6시드에서 사라졌다. 최종 `AB_base+DW_cell+TMC1` 가중은
`.354/.646/.000`, 점수는 기존 두 멤버와 같은 902.63이다. 전반→후반에서도 TMC
가중 0이었다. MC1은 현행 위 +1.36뿐이고 반대 반분에서 가중 0이라 제출하지 않는다.

`std-k 40`도 실제 제출 표면(val2024, drop 없음, 4070 8시드)에서 닫혔다.
`SK2_k40−VB2_base = -1.954`(SE 2.377, t=-.822), 현행 블렌드 base 완전교체
-0.564, 절반교체 +0.194이며 절반교체 후반기 효과가 -0.305라 제출하지 않는다.

### Best Configuration

```text
멤버      cat_v14f × 8시드 (이진)  +  cat_ZD5 × 6시드 (실패모드 셀 다중분류 depth5)
가중      base 0.45 / cell 0.55
후처리    SLOPE 1.0416 (로짓 기울기) → SHIFT 0.0052 (상수 차감)
피처      --feat-v2 --te p,pc,ph,b,pi --te-dev --feat-std --std-k 80
          --std-to-prior --std-season-prior --feat-domain --feat-skill-pc
학습      --lr 0.01 --es 500 --depth 8 --l2 10 --refit-mult 1.5
추론      33.7초 / 600초 (6 vCPU, 245,789행)
```

### LB 환산 (중요)

**`LB ≈ 편향제거 로컬 점수 + 128.4`**

원점수 기준 오프셋(153→155→157)은 가중을 바꾸면 밀린다 — 원점수가 수준 변화를
벌점으로 먹는데 SHIFT 가 그걸 되돌리기 때문이다. 편향제거 기준은 128.13→128.64 로 안정.

---

# 남은 여지 — 실측

미학습 2024 표면에서 Brier 를 분해했다(20분위):

```
신뢰도 (후처리로 고칠 수 있음)   +13.1     ← SLOPE 가 이미 +3 을 먹음
해상도 (모델 실력)              +882.8
```

**후처리로 남은 건 10점 미만.** 점수를 올리려면 모델이 더 잘 **변별**해야 한다.

평가 구조의 비용:

```
랜덤 4-fold CV (같은 시즌 안 보간)   2076
미학습 다음 시즌 예측                 880
                              차이  1196
```

상위권과의 32점 차이는 이 1196 안에서 벌어지는 다툼이다.

---

# 열려 있는 것

| 축 | 상태 | 비고 |
|---|---|---|
| **F리그 해상도 구멍** | **OPEN — 확인됐고 레버가 없다** | 정직 순위해상도 hon/norm: F 507.8 vs R 839.3(base), F 429.8 vs R 861.8(**cell**). pred_sd F .0319 vs R .0454 — 모델이 F에서 변별을 포기하고 평균으로 도망친다. 셀 멤버가 전체로는 더 강한데(890.63 vs 876.90) F에서는 더 나쁘다 |
| std-k 80 → 40 | CLOSED | 제출 표면 −1.954 |
| ~~47열 토큰 디코더~~ | **CLOSED** | 4팔 전부 margin −75~−87, 가중 0.00. rms .0321~.0341(역대 최고)인데도 D 가 Dmax 보다 빨리 커진다 — 잡음 다양성은 이득으로 환산되지 않는다 |
| ~~2019~2020 절제~~ | **BANNED** | −95.09 (t=−46.0). 행만 빼는 게 아니라 fpipe.fit 앞이라 asof 차분 앵커까지 잘린다 |
| ~~정보량 0 컬럼 삭제~~ | **CLOSED** | li·WE 두 개는 잔차 정보량 0이 실측됐는데도 삭제가 −3.50. 증명된 중복도 못 뺀다 |
| ~~상황축 파생(--feat-v4)~~ | **CLOSED** | −18.72 (t=−6.05). 파생 0인 21열은 공백이 아니라 정답이다 |

**cold-start는 구멍이 아니다** — 2024의 24.3%(61,724행)가 직전 시즌 미출현 투수라
TE 18개가 수축 상태인데, hon/norm이 warm보다 **높다**(959.1 vs 811.7). 사전 게이트
0.6 대비 1.18. `te-cold-start-segment` CLOSED.

# 최근에 닫힌 것 (2026-08-07~08)

기각: Optuna · GPBoost · trackman · 세그먼트 드리프트 · LightGBM · 피처 부분공간 ·
refit 배수 2.0(18시드) · te-k 축별 수축 · **셀 × 카운트 맥락** · **2스트라이크 라우팅** ·
F리그 분리 · isotonic 계열 전반 · 시드 확장

소진: 셀 기하(두 번째 멤버 +0.34, d4·c16 가중 0)

---

# Current Hypothesis

`RT1 roster-transition`은 단일시드 양수였지만 6/8시드에서 승격 실패했다. A100은
앙상블 +8.28, 페어 +7.75(SE 3.68, t=2.10)로 채택선 2.4 미달. 4070은 페어
−0.001(SE 2.279, t≈0), 현행 블렌드 base 교체 centered +0.585 / 고정 제출 변환
+0.163에 불과하고 후반기 −1.337이다. 현행 v18을 유지하고 다음 직교 축이 필요하다.

기존 `F 2023=ABS 1년차` 해석은 공식 연혁과 실패모드 감사로 폐기했다. 퓨처스 ABS는
2020년부터 운영됐고 F reverse가 2022 .0497→2023 .2988로 뛴 것은 데이터/타깃 체제
단절이다. 현행 v18 SHIFT 값은 새 검증 없이 바꾸지 않는다.

H1 dynamic hierarchy는 source 2022→2023R +8.03이 target 2024 -19.09로 반전해 닫혔다.
OR1 segmentation fault는 PairLogitPairwise group64의 pair 메모리 폭증으로 확인했다.
group16은 50iter smoke를 통과해 A100 정식 `OR2_rank16`을 실행 중이다. 4070의
`OR2_rank16p`는 refit 중 Windows native access violation로 끝나 A100 결과만 쓴다.
baseline-col의 refit/test/제출 누락은 수정했고 4070 `BC1_offset`으로 재검정한다.

신규 모델 파일럿은 모두 음수로 종료했다. MLP-PLR은 미학습 최고 641.29였고
sigma1 517.60, sigma10 426.58로 악화했다. content two-tower도 미학습 675.32 /
제출 표면 699.67에 그쳐 두 축 모두 6시드 확장 없이 닫았다.

G0 topology-only graph는 A100 미학습 2024 `866.90`(동일 seed AB 대비 -1.93),
4070 제출 표면 `902.06`(동일 seed VB2 대비 -9.16)으로 종료했다. 두 표면 모두
음수라 G1 GraphSAGE 승격 없이 그래프 축을 닫았다.

> **Claude 최종 판정: 증류 기각.** 판정 표면 `+38.43` 의 85% 가 교사만 본 F 구체제
> 행이었고(`DT5_seq` 로 +4.6~7.0 붕괴), 제출 표면에서는 base 대비 −10.19 에 블렌드
> 가중 0.00 이다. base 와 rms 0.0067 로 셋 중 가장 닮았다 — 새 기하가 아니라 base 의
> 매끄러운 복사본이었다.
>
> 후처리·하이퍼·증류·`std-k 40`까지 닫혔다. 현재 검증 큐에는 목표 1105까지
> 필요한 +11을 낼 근거 있는 축이 없다. 현행 v18을 보존하고 새 직교 가설이 필요하다.

# Next Experiment

## OB1 Ordered boosting — 양 표면 실패

현재 CatBoost는 ordered target statistics는 사용하지만 대규모 데이터의 boosting
scheme은 기본 `Plain`이다. `--boosting-type Ordered`를 검증·refit 양쪽에 같은
단일 플래그로 배선했다. 피처·손실·가중은 기준선과 동일하다.

- A100: `OB1A_ordered`, seed3, val2023→test2024, `--drop-f-pre 2022`
- 4070: `OB1V_ordered`, seed42, val2024, drop 없음
- 두 precheck exit 0. A100 `830.75−868.83=−38.08`, 4070
  `900.49−911.22=−10.73`으로 모두 음수. 확장하지 않는다.
- 실행 전 BR1 고정 리그 보정은 합법 점수 `+0.239`, 후반 `−3.145`로 탈락했다.

## FW1 cell-window — 다음 단일시드 게이트

binary base에서 `--feat-window`는 6시드 앙상블 +3.75로 방향은 양수였으나
t=1.42였다. 최근 1/3/5경기 산포가 성공 이진값보다 실패형태
(middle/ball/reverse) 분류에 더 직접적으로 작용하는지, 현행 depth5 cell에
`--feat-window` 하나만 추가해 양 표면 단일시드로 먼저 본다.

결과는 A100 동일 seed3 `DW_cell` 대비 centered `+0.51`, 4070 동일 seed42
`ZD5` 대비 `−5.85`다. 두 번째 표면이 음수라 확장하지 않는다.

## RC2 cell-roster — 다중시드 확장 중

RT1 이진모델과 ZD5를 사후 블렌드하는 대신, 로스터 전환 12피처를 depth5 실패모드
셀 내부에 직접 넣었다. RC1 단일시드는 A100 `+3.40`, 4070 `+4.45`로 양 표면 +3을
통과했다. A100 6시드/4070 8시드를 채운 뒤 t≥2.4와 현행 블렌드 증분을 판정한다.

## 2026-08-09 04:37 야간 분리 실행

### RT1 다중시드 최종 — 제출 게이트 실패

- A100 `RT2A_roster` 6시드: 앙상블 `+8.28`, 페어 `+7.75`, SE `3.68`,
  `t=2.10`으로 채택선 `2.4` 미달.
- 4070 `RT2V_roster` 8시드: 페어 `−0.001`, SE `2.279`, `t≈0`.
  현행 base+cell에서 base 완전교체 centered `+0.585`, 절반교체 `+0.463`;
  고정 SLOPE/SHIFT 적용 시 각각 `+0.163/+0.272`뿐이다.
- 4070 반분은 완전교체 전반 `+2.300`, 후반 `−1.337`. 8시드 ensemble 자체는
  base보다 raw `−3.384`, centered `−0.149`다. F→R 기여 `+0.948`은 남았지만
  same-cont `−3.255`, 후반 전체 `−4.498`이 상쇄했다.
- v14f·ZD5·RT2V 학습집합 지문 `.5401750413` 일치. 비교는 유효하지만 개선이
  작고 비전이적이다. v10 zip은 생성하지 않고 현행 v18을 유지한다.

- A100: `RT1A_roster`(미학습 2024) → 정상 종료 시 `PG1A_pg` 순차 실행.
- 4070: `RT1V_roster`(val2024 제출 표면) → 정상 종료 시 `PG1V_pg` 순차 실행.
- RT1은 타깃 없이 시즌 S 이전 투수의 직전 등장 리그·공백과 R/F 이력량만 쓴다.
  테스트 행끼리 상태를 갱신하지 않으며 cutoff/행 독립 검문을 양 머신에서 통과했다.
- PG1은 이미 구현돼 있으나 현행에서 빠진 `pitcher_id×game_type` expanding TE를
  단독 변경으로 더한다. 둘 다 단일시드 게이트이며 자동 다중시드 확장은 하지 않는다.
- 로그: A100 `out/rt_pg_a100.log`, 4070 `out/rt_pg_4070.log`.

### Codex 1차 분석 (잠정)

| 축 | A100 미학습 2024 (동일 seed3) | 4070 val2024 (동일 seed42) | 잠정 의견 |
|---|---:|---:|---|
| RT1 roster | `884.49−868.83 = +15.66` | `915.53−911.22 = +4.30` | 양 표면 양수, 다중시드 승격 후보 |
| PG1 pitcher×league TE | `858.11−868.83 = −10.72` | `912.55−911.22 = +1.33` | 전이 불일치, 확장 가치 낮음 |

RT1은 A100 centered `+15.72`, 4070 centered `+8.14`, 예측 차이 RMS는 각각
`.00634/.00528`이다. 4070의 전체 `+4.30` 중 F→R 기여가 `+3.43`으로 가설과
맞았으나 시즌 전반 `+6.91`, 후반 `−2.61`로 갈렸다. A100에서는 same-cont가
`+13.57`을 차지해 기전이 동일하지 않다. 따라서 단일시드로 제출하지 않고, RT1만
동일 표면 6/8시드 페어 확장 후 t값과 현행 base+cell 블렌드 증분을 확인한다.

## Codex 단독 실행 큐 (임시 운영)

0. **O1 기각:** `OG1_cell14` 6시드. 2023 R 반분 source에서는 cells가 scalar보다
   최소 +101.32였으나 미학습 2024 R route에서 −52.38. posterior 잔차가 시즌 비전이.
1. **OR2 종료:** group64는 pair 전개 메모리 폭증, group16 full refit도 4070과
   A100 양쪽에서 native crash. no-refit smoke만 통과했으며 산출물 없는 현재 ranking
   구현은 성능 비교에 쓰지 않고 보류한다.
2. **4070 완료:** `BC1_offset=899.31`, 동일 seed `VB2_base=911.22` 대비 −11.91.
   수정된 baseline 경로에서도 개선되지 않아 확장 없이 닫는다.
3. **RT1 종료:** A100 t=2.10, 4070 paired≈0, 고정 제출 블렌드 +0.16·후반기 음수.
   단일시드 +15.66/+4.30은 시드 선택편의였으며 새 zip을 만들지 않는다.

1. **완료:** `TH2_hl2`, 4070 val2024 8시드, drop 없음. 지문 일치(exit 0),
   오류 없음. `VB2_base` 대비 paired +0.164(SE 1.760, t=.093)으로 승격 실패.
2. **4070 / 완료:** 기존 예측만으로 logit blend·세그먼트 resolution·연도 전이 검문.
   logit `+0.021`, 타자경험 `-47.618`, 투수×타자손(k=100) `-191.952`로 모두 GPU 승격 실패.
3. `TW1_window`는 앙상블 +3.75, 페어 +3.42(SE 2.40, t=1.42), blend +3.78로
   보류. 추가시드보다 `TA1_anchor` 신규 축을 우선한다.
4. `TA1_anchor`는 앙상블 +1.07, 페어 -0.58(SE 2.40, t=-.24), blend +2.06으로
   약해 후순위 보류. 다음 신규 GPU 축으로 승격하지 않는다.
5. RMSE, 기존 TabM 단순 재블렌드, 현재 형태의 skill-hand는 닫힌/중복 축이라 재실행하지 않는다.
6. `TH1_hl2`는 A100 판정 표면에서 앙상블 +7.15, paired +6.03(SE 1.82,
   t=3.32), blend +7.29로 채택. `TH2_hl2`가 제출 표면 전이 여부를 결정한다.
7. `TH2_hl2` 고정 대체는 현행 블렌드 대비 +0.610뿐이며 양방향 시즌 반분에서
   각각 -1.573/-1.112. 새 제출 zip을 만들지 않고 현행 v18을 유지한다.
8. 현행 `blendv9_0808_0126.zip` 재검증 완료: 행 독립 최대차이 0, 모델 14개,
   5행 2.4초, 평가규모 245,789행 29초(한도 600초), 종합검증 exit 0.
9. `SK2_k40` 4070 제출 표면 8시드 완료: 지문 일치(exit 0), 페어 -1.954
   (SE 2.377, t=-.822), 현행 블렌드 완전교체 -0.564, 절반교체 +0.194.
   절반교체 전반기 +0.417/후반기 -0.305로 제출 승격 실패. GPU 양쪽 유휴.

Claude 검토는 현재 진행 조건이 아니다. 사용자가 복귀를 지시하기 전까지 Codex가 수치와
규칙을 근거로 다음 큐까지 결정한다.

## Claude 검토 → P3 방향 확정

### Goal

Codex 실행은 완료됐다. `HANDOFF.md`의 수치·로그·지문을 독립 검토해 다음을 정한다:

- `DX2_seq` 최종 기각 및 P3 가중 후보 제외 여부
- DT5 경계값(t=2.36) 추가 시드의 가치 vs 증류 축 종료
- 다음 우선순위를 새 피처·손실·블렌드 다양성 중 어디에 둘지

### Keep Fixed

```text
P2'   4070 / val2024 / 8시드 / 학습집합 지문 0.5401750413
P2'-C A100 / val2023→test2024 / 6시드 / 양쪽 --drop-f-pre 2022
AB2_base·DT4_seq(P2'-B)는 구조적 무효이므로 비교 금지
```

### Success Criteria

```text
Codex 1차 분석은 잠정이며 SETTLED 추가·제출 결정은 하지 않는다.
Claude가 코드·LEDGER·동일 머신/표면 조건을 확인한 뒤 최종 판정한다.
```

---

# Decision Log

## 2026-08-09 관계형 신규 축

상세 설계는 [`docs/HIERARCHY_GNN_PLAN.md`](docs/HIERARCHY_GNN_PLAN.md)에 고정했다.
2024 과거 그래프 감사에서 exact-pair hit 51.2%, 투수 hit 80.1%, 활성 투수 이웃 Jaccard
중앙값 0.360을 확인했다. 첫 실험은 full GNN이 아니라 동결 topology-only graph feature의
미학습 2024 gate이며, 통과할 때만 inductive GraphSAGE로 확장한다. 테스트 행 사이 message
passing 또는 temporal state 갱신은 행 독립 위반으로 금지한다.

| 날짜 | 결정 |
|---|---|
| 08-07 | 오프셋의 정체는 SHIFT 가 아니라 **구조적 이득**(한 시즌 더 학습) |
| 08-08 | 비교군은 **같은 머신**에서 만든다 (11점 차 실측) |
| 08-08 | LB 환산 좌표를 원점수 → **편향제거**로 교정 |
| 08-08 | 손실 지도의 F리그 구멍은 **착시** (MSE 로는 R 보다 낮음) |
| 08-08 | 후처리 여지 총 +13.1 확인 → 상수 튜닝 종료 |
| 08-08 | **증류 기각.** 판정 표면 이득의 85% 가 교사/학생 데이터 비대칭. 제출 표면 −10.19, 가중 0.00 |
| 08-08 | 판정 표면(val2023)은 `--drop-f-pre 2022` 를 **요구**하고 제출 표면(val2024)은 **안 쓴다**. 명령을 표면 간 복사 금지 |

## 2026-08-15 P3-C2 독립 로컬 재현

주 판정인 5070 fixed-core `-1.222` 기록 후, 현재 RTX 3070 Ti에서도 fresh
base/control로 독립 재현했다. fixed core `890.294435 -> 885.486834`, delta
**-4.807601**이며 모든 확인 세그먼트가 음수였다. 머신 간 수치는 합치지 않지만
FAIL 방향은 일치한다. 상세:
[`docs/P3C2_LOCAL_REPLICATION_20260815.md`](docs/P3C2_LOCAL_REPLICATION_20260815.md).
