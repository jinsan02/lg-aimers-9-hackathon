# 잔여 축 안건 — 2026-08-08 조사 결과

## 2026-08-08 Codex 실행 갱신

- **T1-1 종료:** logit 평균은 probability 평균 대비 centered `+0.021`로 무효.
- **T1-2 완료:** F와 시즌 후반은 안정적 순위 resolution이 얇지만, 이를 곧바로 상수
  보정 근거로 쓰지 않는다.
- **T1-3 종료:** 타자 경험 고정 보정의 2023→2024 전이 `-47.618`.
- **T2-1 종료:** 투수×타자손 잔차 전이 k=100 `-191.952`, 공통그룹 상관 `+.011`.
  동일 시즌 교차적합의 `+31`만으로 GPU 실험에 올리지 않는다.
- **T2-2 보류:** A100 `TW1_window` 앙상블 +3.75, paired +3.42(SE 2.40,
  t=1.42), 최적 blend +3.78. 양수지만 채택 기준 미달.
- **T2-3 수정:** anchor exposure와 missing flag는 서로 다른 변경이므로 반드시 분리한다.
  missing은 E73 debut/return 실패와 겹치므로 후순위다.
- **T2-4 삭제:** RMSE는 E55/E93 중복이며 채택 근거 없음.
- **T2-5 후순위:** halflife=2 과거 결과는 `+7.255, t=2.120`이지만 no-refit 및
  올바른 미학습 2024 표면이 아니다.
- **T3-1 중복:** E21에서 기존 TabM greedy weight 0. 현재 121/std로 새로 학습할 때만 새 축.
- **T3-3 부분 중복:** `train_mtnn.py` E140이 shared trunk+aux heads를 이미 구현했다.
  marginal-consistent 제약만 신규 확장이다.
- **T2-3 진행 중:** A100 `TA1_anchor` 6시드. 순수 과거 n0/S0 노출만 넣고
  신규선수 missing 플래그는 다음 실험으로 분리했다.

> 증류 기각 후 "남은 게 있나"를 네 방향(코드 감사·데이터 감사·문헌 서치·손실 각도)으로
> 훑은 결과. **실행 지시가 아니라 안건이다.** 우선순위와 게이트만 정해 뒀다.
> 근거는 각 항목에 실측/문헌으로 붙였다.

## 왜 이 목록이 이렇게 생겼나 — 구조적 사실 하나

**해상도(resolution)는 단조 변환에 불변이다.** 그래서 "고치는 방법이 단조 사상 하나"인
축은 전부 신뢰도 예산 **+13.1** 안이고 이미 닫혔다(isotonic·SLOPE·SHIFT·꼬리 확장).

남은 점수는 두 곳에서만 나온다:
1. **예측의 순위(ordering)를 바꾸는 것** — 새 피처, 새 모델 기하
2. **조건부(세그먼트별) 오프셋** — 이건 전역 +13.1 상한에 **안 묶인다**

지금 가장 저평가된 게 2번이다.

---

# T1 — 로컬 즉시 (GPU 불필요, `out/*_val_preds.npz` 로 끝난다)

## T1-1. 블렌드를 확률 평균 → **로짓 평균**으로

- **근거**: 단순 확률 평균은 예측을 0.5 쪽으로 눌러 sharpness 를 깎는다
  (arXiv 2605.18696 — "naive averaging" 이 캘리브레이션을 악화). 우리 블렌드 예측
  범위가 **0.316~0.666, sd 0.046** 으로 이미 좁다. 로짓 평균이 그 직접 처방이다.
- **왜 닫힌 축이 아닌가**: 사후 확률 보정이 아니라 **블렌드 연산자 선택**이다.
  isotonic(E30 −21)과 다른 범주다.
- **비용**: 코드 1줄. `script_blend_v9.py` 의 가중합을 로짓 공간에서.
- **판정**: `VB2_base`·`ZD5` val2024 예측으로 편향제거 점수 비교. 현행 961.63 대비.
- **주의**: SLOPE 1.0416 이 이미 로짓 기울기를 건드리고 있다. **둘을 동시에 바꾸지 말 것** —
  로짓 평균으로 바꾸면 SLOPE 를 다시 재야 한다(제출 하나에 변경 하나).

## T1-2. **세그먼트별 해상도** 도구 — 지금 이걸 재는 코드가 없다

- **공백**: `reliability.py` 는 전역만, `loss_map.py` 는 세그먼트 BSS 만 낸다.
  손실 분해가 "해상도 +882.8" 이라 했는데 **그게 어디서 얇아지는지 본 적이 없다.**
- **실측 (조사 중 확인)**: 10분위 해상도 —
  `3월 1461 → 9월 579` / `R 946 vs F 753`(예측 sd R .0462 vs **F .0384**) /
  `투수경험 ~3k 1014 vs 10k+ 736`. **F 는 MSE 로는 멀쩡한데 예측을 17% 덜 벌린다** —
  변별을 포기하고 평균으로 도망친 서명이다.
- **필수 방법론**: 세그먼트 해상도를 그대로 손실로 읽지 말 것. 그 세그먼트의
  **도달 가능한 해상도**(honest_ceiling 식 세그먼트 내 교차적합 오라클)와 비교해야
  "모델이 못하는 것"과 "원래 변별이 안 되는 구간"이 갈린다. F 구멍 착시에 한 번 빠졌다.
- **비용**: `reliability.py` 에 mask 인자 추가 + `loss_map.py` 세그먼트 정의 재사용.

## T1-3. **타자 경험 축**(`asof_batter_n`)의 ABS 전후 부호 반전

- **실측 (조사 중)**: 2024 교차적합에서 버킷 5개짜리 축 하나로 **+30.3 BSS**
  (k=0/10/30/100 전부 동일 → 그룹이 커서 수축 무관). `타자경험×카운트` +14~20.
- **잔차**: `~200 PA −0.0231` / `~1k −0.0105` / `~3k·~10k −0.006` / `10k+ −0.0109`
  → 전역 편향 제거 후 **저경험 타자에서 −1.5%p 초과 과대예측**, 완만한 U자.
- **기전 가설**: `~200` 버킷의 상대 성공률이 2019 +21 / 2021 +76 / 2022 +85 →
  **2023 −22 / 2024 −25 (×1000) 로 ABS 에서 부호가 뒤집혔다.** 모델은 구체제 4시즌으로
  학습해 신체제 부호를 못 따라간다. 그리고 `asof_batter_n` 은 2019 부터의 통산 누적이라
  "500 PA" 의 의미가 2019(집계 절단)와 2024(진짜 신인)에서 다르다 — E99 와 같은 계열.
- **왜 안 닫혔나**: honest_ceiling 의 `타자`(batter_id 개체) 축은 −174 로 죽는데
  **경험 계층 버킷은 +30** 이다. 개체가 아니라 계층이 비어 있다.
- **⛔ 게이트**: 이건 **시즌 내 A/B** 다. E123 이 여기서 −4.07 을 태웠다.
  **2023 적합 → 2024 적용 이전성 검정을 통과하지 못하면 즉시 폐기.**
- **비용**: `honest_ceiling.py` AXES 에 `_bn` 추가 + `drift_seg.py` 이전 검정 재사용.

## T1-4. 기록 공백 메우기 (돈 안 드는데 정보가 없다)

- `extreme_subgroup.py` — 결과가 **어느 문서에도 없다**. 10분이면 돌린다.
  "왜 확신하는 행이 하나도 없는가"(예측 범위 0.316~0.666)의 답을 갖고 있다.
- `honest_ceiling` 의 `타자` 행 — 계산되는데 기록이 없다.
- `seg_weight.py` — 실행 흔적 없음.
- `gap_analysis.py` — 상수가 **LB 845.91 / 1등 989** 기준으로 낡았다. 지금 읽으면 오답.
- **`docs/SETTLED.md` 에 13개 축 누락** — 아래 T4 참조.

---

# T2 — GPU 실험 (근거 있고 한 번도 안 돌린 것)

각각 6시드 페어 비교, 판정 표면 `--val-season 2023 --test-season 2024 --drop-f-pre 2022`,
기준선 `AB_base 883.41`(A100). 채택 t ≥ 2.4.

## T2-1. `--skill-axes count,hand` ★ 가장 유력

- **근거**: `honest_ceiling.py` 교차적합 잔차 여지에서 **투수×타자손만 +31.0 (k=100)**
  으로 유일한 양수 축이다. 투수 −48.9, 투수×카운트 −131.1 로 다 음수인데 여기만 남았다.
  그 축의 skill 추정기가 **구현만 되고 한 번도 안 돌았다** (LEDGER 0회, 아카이브 0건).
- **⚠ 구현 함정 2개** (이거 놓치면 조용히 무효가 된다):
  1. `--skill-axes` 는 `--feat-skill-pc` 를 **덮어쓴다**(or 아님, 완전 대체).
     현 제출 설정을 유지하려면 반드시 `--skill-axes count,hand`.
  2. `AXES["hand"]["feat"]` 가 `te_pitcher_batter_hand_*` 를 읽는다 →
     **`--te ...,ph --te-dev` 가 켜져 있어야** 한다. 없으면 크래시가 아니라
     **중앙값 상수로 채워져 조용히 무효**가 된다. 실행 후 그 컬럼의 분산을 확인할 것.
- **반대 근거**: `LEVERS.md:81` "TE `ph` 가 이미 같은 축을 먹고 있다".
- 비용: A100 약 12~15분/시드.

## T2-2. `--feat-window` — 모델에 산포(dispersion) 피처가 **하나도 없다**

- **근거**: 현재 121개 피처가 전부 평균·수준이다. `season_std.add_window()` 가
  prev1/3/5 **중첩 창을 경기1 / 2~3 / 4~5 로 역산 분해**하고 `win_*_sd`·`win_*_range`
  를 만든다. 역산 identity 는 검증 완료(값이 [0,1] 밖인 비율 0.0%). `3·p3 − p1` 같은
  선형결합은 트리가 못 만든다.
- **왜 신호가 있을 수 있나**: 주최측이 "제구 안정성·변동성"을 명시했고, 유일하게
  변동성을 시험한 E101(트랙맨 릴리스 분산)이 **+5.9(t=1.02)로 양수**였는데
  소스(트랙맨)째로 닫혔다. 같은 개념을 **허용된 asof 만으로** 다시 만드는 경로다.
- **미측정 확인**: `grep -r feat-window LEDGER.tsv docs/ *.sh` → **0건**.
  SETTLED 의 `--label-smooth | CLOSED | E118 계열` 은 라벨 평활이지 이 피처가 아니다.
- **닫힌 축과 구분**: E108(form = prev − std, 수준 차 −5.4) · E113(카운트×스타일 −1.07) ·
  E100(시즌 lag −7.6) 과 전부 **다른 대상**(중첩 제거 + 산포).
- 비용: 플래그 하나. `--feat-std` 와 같이 켤 것.

## T2-3. 앵커 잔재 노출 — "당해를 뺀 순수 과거" 층이 없다

- **공백**: `add_std()` 가 `n0`(시즌 시작 시점 통산 투구수), `S0_*`(그때까지 통산 성공수)
  를 merge 해 차분에 쓴 뒤 **전부 drop 한다**. 모델이 가진 건 통산(당해 포함, `asof_*`)과
  당해 시즌(`std_*`) 두 층뿐 — `asof_n − std_n` 같은 뺄셈은 트리가 못 만든다.
- **부수 발견**: `n0` 결측(= 이력 없는 신규 투수)을 `fillna(0)` 로 **조용히 채운다.**
  LEVERS 의 "결측을 채우지 말 것 — CatBoost 가 결측=신규투수를 정보로 쓴다"와 정면 충돌.
  `std_anchor_missing` 플래그가 없다.
- 비용: `season_std.py` drop 직전 2~3열 노출 + 결측 플래그. 코드 3줄.
- 겹침: E100(시즌별 lag, −7.6)과 일부 겹치나 그건 시즌 1개씩 쪼갠 소표본이고
  이건 전 과거를 합친 대표본이라 다르다.

## T2-4. CatBoost 목적함수 `RMSE`(0/1 라벨) 멤버

- **근거**: 0/1 라벨에 대한 제곱오차는 **정확히 Brier 를 직접 최소화**하는 것이고
  squared error 도 strictly proper 다. Buja/Friedman: "boosting loss 가 극단 예측에
  가장 강하게 의존하고, log-loss 가 그 다음, squared error 가 가장 약하다" —
  극단에 덜 휘둘리는 목적함수가 드리프트 하에서 안정할 여지.
- **부수 사실**: CatBoost 는 이진 라벨에서도 `MultiClass` 와 `Logloss` 가 서로 다른
  확률을 뱉는다(issue #807). **지금 블렌드하는 두 멤버의 확률 스케일이 이미 다르고**
  가중이 그걸 흡수하고 있을 수 있다.
- 비용: `--loss RMSE`. 학습 1회. 근거는 원리 수준이고 이 데이터 실측은 없다.

## T2-5. `--te-halflife` 결과 회수 — **이미 돌렸는데 결과가 없다**

`tehl.sh`(H3_hl1/2/4)로 발사된 흔적이 있고 `out/cat_H3_hl*_val_preds.npz` 가
로컬에 남아 있는데 **수치가 어느 문서에도 없다.** 새로 안 돌려도 된다. 계산만 하면 된다.

---

# T3 — 중기 (블렌드 다양성, 3주 남은 일정에서만)

## T3-1. TabM 을 **교체가 아니라 블렌드 멤버로** 재판정

- **판정 기준 오류 가능성**: TabArena(NeurIPS 2025 D&B, 16모델×51데이터셋)는 리더보드
  순위와 **cross-model 앙상블 평균 가중치**를 따로 보고하는데, 가중치 상위 4개가
  **TabM · LightGBM · RealMLP · CatBoost** 다. 저자 명시: "GBDT 를 이기는 모델보다
  **앙상블에서 잘 작동하는 모델**을 찾는 게 더 중요하다."
- 우리 TabM 은 이미 있고 로컬 544.24 였다. **단독 성능으로 기각했다면 기준이 틀렸다.**
- **⚠ 저자 자신의 경고**: "ensemble construction favors models performing better on
  validation than test data" — 홀드아웃 1개로 블렌드 가중을 정하면 그 함정에 빠진다.
  우리가 오늘 셀 멤버에서 본 두 표면 불일치와 같은 현상이다.

## T3-2. MLP-PLR (수치 피처 주기 임베딩 + MLP)

- **근거**: TabReD(ICLR 2025 Spotlight)는 **실제 산업용·시간 분할·대량 피처** 8종으로
  우리 상황에 가장 가깝다. 평균 랭크 **XGBoost 2.2 vs MLP-PLR 2.5** — 사실상 동률.
  시간 분할에서는 GBDT 우위가 더 줄어든다.
- **같은 벤치마크에서 실패한 것**: TabR · ModernNCA · DCNv2 · SNN · Trompt 전부
  평범한 MLP 보다 못했다. **retrieval 계열은 후보에서 빼도 된다.**
- 제약 적합: torch 2.7.1 기본 설치 → requirements 빈 상태 유지 가능. 순수 torch 약 200줄.

## T3-3. 공유 trunk 멀티헤드 (4-way CE + marginalized BCE 공동 손실)

- **근거**: ESMM(KDD 2018)/ESCM²(SIGIR 2022) — 이진 확률 + 관측 가능한 세부 결과 라벨이라는
  **구조가 우리와 동일**하다. 핵심은 **hard parameter sharing + joint loss**.
- 우리 현재: 이진 CatBoost 와 셀 다중분류 CatBoost 를 **따로 학습해 블렌드**한다.
  공유가 전혀 없다. marginalization 을 손실 안에서 강제하면 두 뷰가 학습 중에 서로를 제약한다.
- **근거 한계**: ESMM 은 CTR 도메인이다. TabArena/TabReD/TabZilla 어디에도 정형
  multi-task 실험이 없다 — 이 축은 **정형 벤치마크 근거가 부재**하다.
- **함정**: 4-way softmax 에서 `P(success)=Σ` 를 뽑을 때 필요한 건 canonical calibration
  이고 이건 class-wise 보다 강한 조건이다. 4-way 가 marginal 에서 잘 보정돼 있다고
  가정하지 말 것(arXiv 2107.08353).

---

# T4 — 위생 (실험 아님, 하지만 안 하면 또 중복한다)

## T4-1. `docs/SETTLED.md` 에 **13개 축이 누락**돼 있다

측정은 끝났는데 SETTLED 에 없어서 `precheck.py` 가 못 막는다. LEDGER 는 08-08 01:30
이후 것뿐이라 "LEDGER 0회"가 "안 해봄"으로 오인된다.

```
--feat-prof (E100) · --feat-form (E108 −5.4) · --feat-cross (레버H −1.7) ·
--feat-count (E113 −1.07) · --std-excess (E104 −24.7) · --std-ratio (E110 −38.5) ·
--std-multi-k (±0) · --std-k-mix / --std-k-bat (E112) · --monotone (E65 −28) ·
--keep-ids (E88 −105) · --drop-unstable (E47 −262) · --fill-prev (−3.51) · --feat-v5 (−26)
```

## T4-2. 코드 결함 3건 — 돌리기 전에 고치거나 닫아야 한다

| 플래그 | 문제 | 처리 |
|---|---|---|
| `--baseline-col` | **test 시즌 예측(`train_gbdt2.py:1119`)과 `fpipe.predict` 에 baseline 이 안 걸린다.** 지금 돌리면 test BSS 와 제출 예측이 둘 다 틀린다. 게다가 SETTLED 가 실행 기록 없이 "중립"이라 단정하고 있다 | 고치거나, 근거 없음을 명시 |
| `--drop-unstable` | UNSTABLE 24열이 `asof_*_n` 3개를 다 빼서 `season_std.SPECS` 전부 skip → `fpipe.py:130` **IndexError 확정** | SETTLED BANNED (E47 −262) |
| `--transfer-split` | refit 블록 **앞에서 return** → 제출용 전체 재학습 없음, `--refit-mult`/가중/soft-target 전부 무시, best_iter 가 `transfer_iters` 그대로 → **다른 LEDGER 행과 페어 비교 불가** | SETTLED CLOSED (LEVERS 0.00) |
| `--tm-context` | 입력 CSV 부재 + `tmc_*` 가 `art` 에 안 실려 추론 재현 불가 | SETTLED BANNED (트랙맨 축) |

---

# 조사에서 **닫힌 것으로 확인**된 것 (다시 제안하지 말 것)

| 축 | 근거 |
|---|---|
| `li` · `home/away_win_expectancy` | **정보량 0 실측** — (inning, top_bottom, outs, base_state, score_diff_home) 조합당 고유값 1개, 폭 0.0000. 상황에서 완전 결정되는 함수 |
| 점수 5열 파생 | 자유도 2뿐. `score_diff_pitcher_team` 은 `score_diff_home`×`top_bottom` 으로 결정 |
| `base_state` · `outs` 상호작용 | TE `pbs` 이미 −5.7 (t=−3.3) 기각. 상한도 주자없음 5.8 / 2아웃 3.0 |
| 팀 ID = 포수 대리 | 둘 다 UNSTABLE, E82 기여 0, ABS 이후 프레이밍 효과 소멸 → 2025 이전 안 됨 |
| asof 차분 미적용 컬럼 | **없다.** 통산 누적 13개 전부 적용됨 |
| 투수 개인별 잔차 | honest_ceiling −48.9, 투수×월 −10.5. 투수 개인 축은 더 파지 말 것 |
| 예측 확률 극단 구간 | 블렌드 범위가 0.316~0.666 이라 0.2↓/0.8↑ 행이 **하나도 없다.** 꼬리 확장은 단조 변환 → 해상도 불변 |
| IRM / Group DRO | 정형에서 ERM 대비 **최대 −13.8 AUC** (medRxiv 2026). 우리가 기각한 세그먼트 드리프트와 같은 실패 모드 |
| 최신 시즌 가중 재시도 | RIDER(arXiv 2507.13287) 프레임에서 "풀링이 최적인 레짐"에 해당. 자체 실측 LB −41.5 와 일치 |
| GRANDE · TabR · ModernNCA | TabReD 시간분할에서 평범한 MLP 이하 / TabArena 미포함 |
| pseudo-labeling | 2025 평가 데이터 접근 불가 + test 행 간 정보 사용은 실격 |

## 판단 보류 — 근거가 갈린다

**시즌 내부 드리프트(월 축)**: 모델 예측 평균은 3월 0.4989 → 9월 0.4881(−0.011)인데
**실제는 0.5026 → 0.4757(−0.027)** 이다. 교차적합 이득 `월 +7.7` / `월×리그 +15.0`
(F 는 7월 −0.0302 → 9월 +0.0181 로 부호가 뒤집힌다).

**그러나** 시즌 내 기울기(9월−4월, ×1000)가 2019 −3 / 2021 −15 / 2022 −14 /
**2023 +21** / 2024 −21 로 **부호가 안정적이지 않다.** E30 의 월별 보정 −52 와 일치한다.
→ **수준 보정으로는 닫혀 있다.** 살릴 값이 있다면 상수가 아니라 **행 자신의 컬럼으로
계산되는 피처**(`season_progress × 리그`, `season_progress × asof 성숙도`)로 트리에
태우는 쪽이다. 부호 안정성부터 확인하고 아니면 즉시 폐기.
